apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-db
  namespace: vendfinder-staging
  labels:
    app: vendor-db
    environment: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vendor-db
  template:
    metadata:
      labels:
        app: vendor-db
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 80
              preference:
                matchExpressions:
                  - key: role
                    operator: In
                    values:
                      - gateway
      containers:
        - name: vendor-db
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: vendor_db
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vendor-db-credentials
                  key: password
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: vendor-db-data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
      volumes:
        - name: vendor-db-data
          persistentVolumeClaim:
            claimName: vendor-db-data
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-db
  namespace: vendfinder-staging
  labels:
    app: vendor-db
    environment: staging
spec:
  selector:
    app: vendor-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
