apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: vendfinder
  labels:
    app: search-service
    environment: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      imagePullSecrets:
        - name: vendfinder-registry
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 80
              preference:
                matchExpressions:
                  - key: role
                    operator: In
                    values:
                      - prod-app
      containers:
        - name: search-service
          image: registry.digitalocean.com/vendfinder-registry/search-service:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: vendfinder
  labels:
    app: search-service
    environment: production
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - port: 3000
      targetPort: 3000
